[project]
name = "agents"
version = "0.1.0"
description = "Clean multi-lang AI agents"
dependencies = [
    "ruff",
    "pre-commit",
    "google-adk",
    "mypy>=1.19.1",
]
requires-python = ">=3.10"
