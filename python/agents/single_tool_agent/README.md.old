# Time Teller Agent
This agent uses a tool to provide the current time in a specified city.

## Tools
- `get_current_time`: Returns a hardcoded time (for now) for a given city name.

## Usage
Run with:
```bash
make run-agent NAME=time_teller
```
Or serve as a web app:
```bash
make serve-demo
```
